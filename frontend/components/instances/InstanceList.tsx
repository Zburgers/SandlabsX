import Link from 'next/link';
import type { InstanceSummary } from '../../lib/capsule-types';
export function InstanceList({ instances }: { instances: InstanceSummary[] }) { return <section className="rounded-xl border border-slate-700 bg-slate-900/60 p-5"><h2 className="text-lg font-semibold">Instances</h2><ul className="mt-4 space-y-3">{instances.length ? instances.map(instance => <li key={instance.id} className="rounded-lg border border-slate-700 p-3"><Link href={`/instances/${instance.id}`} className="font-medium hover:text-cyan-200">{instance.name}</Link><p className="mt-1 text-xs text-slate-400">Desired: {instance.desiredState} · observed: {instance.observedState || instance.state}</p></li>) : <li className="text-sm text-slate-400">No instances are available.</li>}</ul></section>; }
