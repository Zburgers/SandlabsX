export function ValidationPanel({ issues, onFocusPath }: { issues: Array<{ path: string; message: string }>; onFocusPath?: (path: string) => void }) { return <section className="rounded-xl border border-slate-700 p-4"><h2 className="font-semibold">Validation</h2>{issues.length ? <ul className="mt-3 space-y-2">{issues.map(issue => <li key={`${issue.path}-${issue.message}`}><button type="button" onClick={() => onFocusPath?.(issue.path)} className="text-left text-sm text-amber-200 underline">{issue.path}: {issue.message}</button></li>)}</ul> : <p className="mt-3 text-sm text-emerald-300">No current validation issues.</p>}</section>; }
