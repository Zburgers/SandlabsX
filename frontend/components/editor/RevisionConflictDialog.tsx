import { ActionButton } from '../ui/ActionButton';

export function RevisionConflictDialog({ onReload, onDismiss }: { onReload: () => void; onDismiss: () => void }) { return <section role="alertdialog" aria-labelledby="conflict-title" className="surface-raised rounded-[var(--radius-lg)] p-5"><p className="eyebrow text-warning">revision conflict</p><h2 id="conflict-title" className="mt-2 text-lg font-semibold">This draft changed elsewhere</h2><p className="mt-2 max-w-xl text-sm leading-6 text-[var(--ink-soft)]">Reload the authoritative revision before continuing. SandLabX will not silently replace your local edits.</p><div className="mt-5 flex gap-2"><ActionButton onClick={onReload}>Reload revision</ActionButton><ActionButton tone="quiet" onClick={onDismiss}>Keep local view</ActionButton></div></section>; }
