import type { CapsuleNode } from '../../lib/capsule-types';
export function NodeInspector({ node }: { node?: CapsuleNode }) { return <aside className="rounded-xl border border-slate-700 p-4"><h2 className="font-semibold">Inspector</h2>{node ? <><p className="mt-3">{node.displayName || node.image}</p><p className="text-sm text-slate-400">Profile {node.workloadProfile}</p></> : <p className="mt-3 text-sm text-slate-400">Select a node to inspect its authored interfaces.</p>}</aside>; }
