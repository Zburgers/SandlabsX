import type { CapsuleProfile } from '../../lib/capsule-types';
export function NodePalette({ profiles, onAdd, disabled }: { profiles: CapsuleProfile[]; onAdd: (profile: CapsuleProfile) => void; disabled?: boolean }) { return <aside aria-label="Workload profiles" className="rounded-xl border border-slate-700 p-4"><h2 className="font-semibold">Profiles</h2>{profiles.map(profile => <button key={profile.id} disabled={disabled} type="button" onClick={() => onAdd(profile)} className="mt-3 block w-full rounded border border-slate-600 p-2 text-left hover:border-cyan-300 disabled:opacity-50">{profile.name}<span className="block text-xs text-slate-400">{profile.interfaces.map(item => item.id).join(', ')}</span></button>)}</aside>; }
