'use client';
import { useState } from 'react';
import type { ImpactPreview } from '../../lib/capsule-types';
export function DestructiveActionDialog({ instanceName, action, impact, onConfirm }: { instanceName: string; action: 'reset' | 'destroy'; impact: ImpactPreview; onConfirm: (payload: { impactToken: string; idempotencyKey: string }) => void }) { const [confirmation, setConfirmation] = useState(''); const label = action === 'reset' ? 'Reset instance' : 'Destroy instance'; return <section role="alertdialog" aria-labelledby="impact-title" className="rounded-xl border border-red-400/50 bg-red-950/20 p-5"><h2 id="impact-title" className="font-semibold">{label}</h2><p className="mt-2 text-sm text-slate-300">{impact.summary}</p><label className="mt-4 block text-sm" htmlFor="instance-confirm">Type {instanceName} to continue</label><input id="instance-confirm" value={confirmation} onChange={event => setConfirmation(event.target.value)} className="mt-1 w-full rounded border border-slate-600 bg-slate-900 p-2" /><button type="button" disabled={confirmation !== instanceName} onClick={() => onConfirm({ impactToken: impact.token, idempotencyKey: crypto.randomUUID() })} className="mt-4 rounded bg-red-400 px-3 py-2 font-semibold text-slate-950 disabled:opacity-50">{label}</button></section>; }
