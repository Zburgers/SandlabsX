'use client';
import { ActionButton } from '../ui/ActionButton';
export function NodeControls({ state, onAction }: { state: string; onAction: (action: 'start' | 'stop') => void }) { const running = state === 'RUNNING'; return <section className="surface rounded-[var(--radius-lg)] p-5"><p className="eyebrow">lifecycle</p><h2 className="mt-2 font-semibold">Instance controls</h2><p className="mt-2 text-sm text-[var(--ink-soft)]">Actions submit durable intent. Observed state may update after the request is accepted.</p><div className="mt-5 flex gap-2"><ActionButton disabled={running} onClick={() => onAction('start')}>Start instance</ActionButton><ActionButton tone="secondary" disabled={!running} onClick={() => onAction('stop')}>Stop instance</ActionButton></div></section>; }
