import type { Operation } from '../../lib/capsule-types';
export function OperationStatus({ operation }: { operation?: Operation }) { if (!operation) return null; return <section aria-live="polite" className="rounded-xl border border-slate-700 p-4"><div className="flex justify-between"><strong>{operation.type}</strong><span>{operation.state}</span></div>{operation.progress !== undefined && <div className="mt-3 h-2 overflow-hidden rounded bg-slate-800"><div className="h-full bg-cyan-300 transition-all" style={{ width: `${operation.progress}%` }} /></div>}{operation.error && <p className="mt-3 text-sm text-red-300">{operation.error.message} ({operation.error.code})</p>}</section>; }
