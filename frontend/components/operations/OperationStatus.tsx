import type { Operation } from '../../lib/capsule-types'; import { StatusSignal } from '../ui/StatusSignal';
export function OperationStatus({ operation }: { operation?: Operation }) { if (!operation) return <section className="surface rounded-[var(--radius-lg)] p-5"><p className="eyebrow">operations</p><h2 className="mt-2 font-semibold">No active operation</h2><p className="mt-2 text-sm text-[var(--ink-soft)]">Accepted lifecycle and checkpoint work appears here with durable progress.</p></section>; return <section aria-live="polite" className="surface rounded-[var(--radius-lg)] p-5"><div className="flex justify-between gap-4"><div><p className="eyebrow">operation</p><strong className="mt-2 block">{operation.type || 'Operation'}</strong></div><StatusSignal label={operation.state} tone={operation.error ? 'danger' : 'info'} /></div>{operation.progress !== undefined && <div className="mt-5 h-1.5 overflow-hidden bg-[var(--surface-strong)]" role="progressbar" aria-valuenow={operation.progress} aria-valuemin={0} aria-valuemax={100}><div className="h-full bg-accent transition-[width]" style={{ width: `${operation.progress}%` }} /></div>}{operation.error && <p className="mt-3 text-sm text-danger">{operation.error.message} <span className="mono">({operation.error.code})</span></p>}</section>; }
