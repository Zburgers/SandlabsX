import Link from 'next/link';
import type { CapsuleDraft } from '../../lib/capsule-types';
export function CapsuleList({ capsules }: { capsules: CapsuleDraft[] }) { return <section className="rounded-xl border border-slate-700 bg-slate-900/60 p-5"><div className="flex items-center justify-between"><h2 className="text-lg font-semibold">Capsules</h2><Link href="/capsules" className="text-sm text-cyan-300">Open workspace</Link></div><ul className="mt-4 space-y-3">{capsules.length ? capsules.map(capsule => <li key={capsule.id} className="rounded-lg border border-slate-700 p-3"><Link className="font-medium hover:text-cyan-200" href={`/capsules/${capsule.id}/edit`}>{capsule.document.metadata.displayName || capsule.document.metadata.name}</Link><p className="mt-1 text-xs text-slate-400">{capsule.status} · revision {capsule.revision}</p></li>) : <li className="text-sm text-slate-400">No drafts are available.</li>}</ul></section>; }
