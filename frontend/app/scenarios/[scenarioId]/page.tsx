'use client';
import { useParams, useSearchParams } from 'next/navigation'; import { ScenarioRunner } from '../../../components/scenarios/ScenarioRunner';
export default function ScenarioPage() { const { scenarioId } = useParams<{ scenarioId: string }>(); const instanceId = useSearchParams().get('instanceId') || ''; return <div className="mx-auto max-w-4xl px-4 py-7 sm:px-7 sm:py-10"><ScenarioRunner instanceId={instanceId} scenario={{ id: scenarioId, title: 'Scenario progress', stages: [] }} /><p className="mt-4 text-xs leading-5 text-[var(--muted)]">Instructor console access, when granted, remains visible to the learner and is recorded by the server audit trail.</p></div>; }
