# Capacity

Admission is the authority for CPU, memory, storage, TAP, MAC, segment, and console allocations. When capacity is exhausted, release resources only through a durable stopped or destroy transition. Never reclaim a live reservation manually.
