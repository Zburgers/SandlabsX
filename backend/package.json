{
  "name": "sandboxlabs-backend",
  "version": "1.0.0",
  "description": "Backend API for SandLabX network lab management",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "node --test test/*.test.js",
    "test:watch": "node --test --watch test/*.test.js",
    "sandlabx": "node cli/sandlabx.js",
    "image:doctor": "node cli/sandlabx.js image doctor",
    "image:list": "node cli/sandlabx.js image list",
    "lab:validate": "node cli/sandlabx.js lab validate",
    "db:migrate": "node scripts/migrate.js up",
    "db:migrate:down": "node scripts/migrate.js down",
    "db:migrate:create": "node-pg-migrate create --migrations-dir migrations --migration-filename-format index --migration-file-language cjs",
    "db:check": "node scripts/check-schema.js",
    "db:test-legacy-upgrade": "node scripts/test-legacy-upgrade.js",
    "architecture:inventory": "node scripts/check-architecture.js inventory",
    "architecture:check": "node scripts/check-architecture.js enforce",
    "check": "npm test && node -c server.js && node -c middleware/rateLimit.js && node -c modules/migrationRunner.js && node -c modules/imagePipeline.js && node -c modules/labSpec.js && node -c cli/sandlabx.js && node -c scripts/migrate.js && node -c scripts/check-schema.js && node -c scripts/check-architecture.js && node -c scripts/test-legacy-upgrade.js && node -c migrations/0001_core_schema.cjs && node -c migrations/0002_capsule_control_plane.cjs && node -c migrations/0003_retire_legacy_migration_ledger.cjs && node -c migrations/0004_capsule_platform_schema.cjs && node -c migrations/0005_capsule_platform_constraints.cjs && node -c migrations/0006_image_profile_versions.cjs"
  },
  "keywords": [
    "qemu",
    "kvm",
    "qcow2",
    "guacamole",
    "vm",
    "network-lab"
  ],
  "author": "SandLabX",
  "license": "MIT",
  "engines": {
    "node": ">=20.11"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-jwt": "^8.5.1",
    "express-rate-limit": "^8.2.1",
    "jsonwebtoken": "^9.0.3",
    "multer": "^2.0.0",
    "node-pg-migrate": "8.0.4",
    "pg": "^8.11.0",
    "pino": "^10.1.0",
    "pino-http": "^11.0.0",
    "swagger-jsdoc": "^6.2.8",
    "swagger-ui-express": "^5.0.1",
    "uuid": "^9.0.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "pino-pretty": "^13.1.3"
  }
}
